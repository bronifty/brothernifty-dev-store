<section>
    <h2>{{ section.settings.heading }}</h2>
</section>

{% schema %}
{
    "name": "Test Section",
    "settings": [
        {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "Test Section Heading"
        }
    ],
    "presets": [
        {
            "name": "Test Section"
        }
    ]
}
{% endschema %}</section>